/* 	ExperimentManager.java	Title:			jRacy	Author:			Robert Bell	Description:	This controls the adding of experiments to the system.*/package jRacy;import java.util.*;public class ExperimentManager extends Observable {	//Constructors.	public ExperimentManager()	{	}		//Public methods.	public void addExperiment(Experiment inExperiment)	{		experiments.addElement(inExperiment);	}			public void removeExperiment(Object inObject){		experiments.remove(inObject);	}		public Vector getExperimentList()	{		return experiments;	}		public boolean isEmpty(){		if((experiments.size()) == 0)			return true;		else			return false;	}		public boolean isExperimentNamePresent(String inString){				for(Enumeration e = experiments.elements(); e.hasMoreElements() ;)		{			Experiment exp = (Experiment) e.nextElement();			if(inString.equals(exp.getExperimentName()))				return true;		}				//If we make it here, the experiment name is not present.  Return false.		return false;	}		public void displayExperimentListManager(){		ExperimentListManager tmpRef = new ExperimentListManager();		tmpRef.show();	}		public String getPathReverse(String inString){				//Now set the reverse.		String tmpString1 = inString;		String tmpString2 = "";		String tmpString3 = "";				boolean isForwardSlash = false; //Just to make the reverse string look nicer on										//Unix based systems.				int length = tmpString1.length();												for(int i=(length-1); i>=0; i--){			char tmpChar = tmpString1.charAt(i);						if(tmpChar == '/'){							//This does not really need to get done more than once but ...				isForwardSlash = true;							if(tmpString3.equals(""))					tmpString3 = tmpString2;				else					tmpString3 = tmpString3 + tmpChar + tmpString2;				tmpString2 = "";			}			else if(tmpChar == '\\'){				if(tmpString3.equals(""))					tmpString3 = tmpString2;				else					tmpString3 = tmpString3 + tmpChar + tmpString2;				tmpString2 = "";			}			else{				tmpString2 = tmpChar + tmpString2;			}		}					if(isForwardSlash)			tmpString3 = tmpString3 + "/";				System.out.println("The new string is: " + tmpString3);				return tmpString3;	}	//Instance data.	Vector experiments = new Vector();	}