this directory has (or will have) the files generated by the javadoc command.  
To make these files, run "make javadoc" in the parent directory 
($TAUROOT/tools/src/dms).  Then open the file dms/doc/index.html in a web 
browser.
