#!/bin/@SHELL@
TAUROOT=@TAUROOTDIR@
CONFIG_ARCH=@ARCH@
PERFDMF_HOME=${TAUROOT}/tools/src/dms

XMLSAX_JAR=${TAUROOT}/${CONFIG_ARCH}/lib/xerces.jar
JDBC_JAR=${TAUROOT}/${CONFIG_ARCH}/lib/postgresql.jar
DMS_JAR=${PERFMDF_HOME}/dms.jar
CONFIG_FILE=~/.ParaProf/perfdmf.cfg

java -cp ${PERFDMF_HOME}/classes:${DMS_JAR}:${XMLSAX_JAR}:${JDBC_JAR} examples.AnalysisTest ${CONFIG_FILE}
